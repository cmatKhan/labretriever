{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v labretriever-mcp >/dev/null 2>&1 || echo 'labretriever is not installed or not on PATH. The labretriever MCP server will not be available. Install it with: pip install labretriever (see https://cmatkhan.github.io/labretriever/ for full instructions).'"
          }
        ]
      }
    ]
  }
}
